import { Root, Item, Indicator } from './SegmentGroup';
import type { RootProps, ItemProps, IndicatorProps } from './SegmentGroup';

export type SegmentGroupProps = {
  Root: RootProps;
  Item: ItemProps;
  Indicator: IndicatorProps;
};
export const SegmentGroup = { Root, Item, Indicator };
