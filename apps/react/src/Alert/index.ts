import { Root, Title, Description } from './Alert';
import type { RootProps, TitleProps, DescriptionProps } from './Alert';

export const Alert = {
  Root,
  Title,
  Description,
};

export type AlertProps = {
  Root: RootProps;
  Title: TitleProps;
  Description: DescriptionProps;
};
